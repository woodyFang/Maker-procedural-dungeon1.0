-- Compatibility entry retained for callers of the original renderer module.
-- All rendering now uses the procedural-geometry.md based native pipeline.
return require("Rendering.NativeDungeonRenderer")
